# =============================================================================
# Atlas Custom DB Role Test Harness — Environment Variables
# =============================================================================
# Copy this file to .env and fill in the values below.
#
# Required:
#   MONGODB_URI_ADMIN   — Connection string for an admin-level user on your
#                         Atlas cluster (used by mongosh for data setup/cleanup).
#   ATLAS_PROJECT_ID    — The Atlas project ID where the cluster lives.
#
# The Atlas CLI must be installed and authenticated before running the harness.
# If you use a named Atlas CLI profile, set ATLAS_PROFILE below.
# =============================================================================

# ── Required ─────────────────────────────────────────────────────────────────

# Admin connection string (mongosh).  Example:
# MONGODB_URI_ADMIN="mongodb+srv://admin_user:secretPwd@cluster0.xxxxx.mongodb.net/?retryWrites=true&w=majority"
MONGODB_URI_ADMIN=""

# Atlas project ID (find it in the Atlas UI under Project Settings).
ATLAS_PROJECT_ID=""

# ── Optional — Atlas CLI profile ─────────────────────────────────────────────

# If you have multiple Atlas CLI profiles, set the one to use here.
# Leave commented to use the default profile.
# ATLAS_PROFILE="my-profile"

# ── Optional — Override defaults ─────────────────────────────────────────────

# TEST_DB="role_test_db"
# TEST_ALLOWED_COLLECTION="demo_readonly"
# TEST_DENIED_COLLECTION="other_coll"
# TEST_ROLE_NAME="readDemoCollectionOnly"
# TEST_USERNAME="test_readonly_user"
# TEST_PASSWORD="testReadOnlyPwd123!"
